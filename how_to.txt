First-time setup:

    cd md_to_pdf && npm install

To convert a markdown file to PDF:

    python3 md_to_pdf.py <input.md> [output.pdf]

Example:

    python3 md_to_pdf.py input.md

This will compile input.md directly to input.pdf in your current directory, using a temporary HTML file internally and cleaning it up automatically.


